// Sample JavaScript input for highlighting tests (keywords, template, export, return)
function greet(name) {
  const msg = `Hello, ${name}!`;
  return msg; // return greeting
}

export function square(n) { return n * n; }
export default greet;
